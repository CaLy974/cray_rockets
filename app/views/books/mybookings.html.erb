
<div>

  <div class="container-post">
    <h1 class="title-white">My Bookings</h1>
  </div>

  <div>
      <% @books.each do |book| %>
        <div class="card-post">
          <%# <% if rocket.photo.attached? %>
            <%# <%= cl_image_tag rocket.photo.key, height: 180, width: 230, crop: :fill %>
            <%# <% else %>
            <%# <%= image_tag("Fusée1.png", class:"image-placeholder") %>
          <%# <% end %>
          <div class="card-inpost">
            <p> <%= book.start_date %></p>
            <p> <%= book.end_date %></p>
            <p><%= book.rocket.name %></p>
            <% if book.rocket.photo.attached? %>
             <%= cl_image_tag (book.rocket.photo.key) %>
            <% end %>
            <p><%= book.rocket.capacity %></p>
            <p><%= book.rocket.price%></p>
            <p><%= book.rocket.town %></p>
          </div>
        </div>
      <% end %>

  </div>

</div>
